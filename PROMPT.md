##  Issue: IntersectionObserver Not Working Properly

I’m implementing infinite scrolling in my React app using IntersectionObserver, but I’m facing issues. The observer sometimes triggers multiple times, sometimes doesn’t trigger at all, and in some cases the page keeps increasing continuously.

What is wrong in my implementation? How can I prevent multiple triggers? What is the correct way to use IntersectionObserver in React?
